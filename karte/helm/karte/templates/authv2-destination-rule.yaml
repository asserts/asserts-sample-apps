apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: "authv2"
  namespace: {{ include "karte.namespace" . }}
spec:
  host: "authv2"
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
