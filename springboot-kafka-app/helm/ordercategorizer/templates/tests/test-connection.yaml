apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ordercategorizer.fullname" . }}-test-connection"
  labels:
    {{- include "ordercategorizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ordercategorizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
