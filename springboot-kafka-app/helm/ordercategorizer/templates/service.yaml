apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordercategorizer.fullname" . }}
  labels:
    {{- include "ordercategorizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.jmx.port }}
      targetPort: {{ .Values.service.jmx.port }}
      protocol: TCP
      name: jmx
  selector:
    {{- include "ordercategorizer.selectorLabels" . | nindent 4 }}
