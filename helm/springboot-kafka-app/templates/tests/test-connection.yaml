apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot-kafka-app.fullname" . }}-test-connection"
  labels:
    {{- include "springboot-kafka-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springboot-kafka-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
